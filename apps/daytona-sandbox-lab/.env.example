# copy to .env (bun loads it automatically) or export in your shell
# Leave DAYTONA_API_KEY empty for mock mode (default).
DAYTONA_API_KEY=
# Optional overrides, only used in live mode
# DAYTONA_API_URL=https://app.daytona.io/api
# DAYTONA_TARGET=us
PORT=3000
